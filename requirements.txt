# ─── Web / API ─────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]>=0.14.0,<0.29.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic>=2.0,<2.11.4        # Prefect 2.x incompatible with pydantic >= 2.11.4
pydantic-settings==2.2.1     # 2.9.0 moved ConfigFileSourceMixin, breaking Prefect 2.x

# ─── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# ─── Data Processing ───────────────────────────────────────────────────────────
pandas==2.2.2
numpy>=2.0.0,<2.2.0
openpyxl>=3.1,<3.3        # Required for pandas read_excel (Kaggle XLSX datasets)
pyspark>=3.4.0,<4.0.0     # Bronze→Silver dedup + cleaning (local[*] mode, requires Java 11+)

# ─── Orchestration ─────────────────────────────────────────────────────────────
prefect==2.19.3
griffe==0.49.0       # Prefect 2.x incompatible with griffe >= 1.0 (removed griffe.dataclasses)

# ─── Data Quality ─────────────────────────────────────────────────────────────
# great-expectations was removed: 0.18.x requires numpy<2, which conflicts with the
# bundled salary model (numpy>=2). Add `great-expectations>=1.0` yourself if you add GE suites.

# ─── Kaggle Dataset Ingestion ──────────────────────────────────────────────────
kaggle==1.6.14

# ─── ML Scaffold ───────────────────────────────────────────────────────────────
scikit-learn>=1.5.0,<1.6.0
joblib==1.4.2
# mlflow removed: 2.12.x pins numpy incompatible with numpy>=2 (champion model). Add mlflow yourself if you need it.

# ─── Logging ───────────────────────────────────────────────────────────────────
json-log-formatter==1.0
